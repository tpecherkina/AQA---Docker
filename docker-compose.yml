version: '3.8'
services:
  postgresql:
    image: postgres:12-alpine
    ports:
      - 5432:5432
    environment:
      - POSTGRES_DB=database
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=password
